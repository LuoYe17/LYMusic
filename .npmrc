# ly-music-source 是按 tag 锁定的 git 依赖：
#   "ly-music-source": "git+https://github.com/LuoYe17/ly-music-source.git#v0.1.24"
# npm 12 起默认 allow-git=none，直接拒绝安装，报 EALLOWGIT。
#
# root = 只放行「本项目自己声明的」git 依赖，传递依赖里的 git 引用仍然被拒。
# 不要改成 all：那会把这条供应链限制整个放开。
allow-git=root
