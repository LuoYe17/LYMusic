# Dependabot 配置：只做版本更新 PR（安全更新 PR 保持关闭，漏洞靠人工按需修）

version: 2
updates:
  # npm 依赖按月检查一次；仓库没有 Dockerfile，别再从上游配置里抄 docker 那条空跑
  - package-ecosystem: 'npm'
    directory: '/'
    schedule:
      interval: 'monthly'
